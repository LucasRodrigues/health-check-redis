{
  "name": "health-check-redis",
  "version": "0.0.1",
  "dependencies": {
    "redis": "^2.4.2"
  },
  "scripts": {
    "build": "npm install && npm run style && gulp BUILD",
    "style": "npm install && eslint ./src/*.js --fix"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.1.2",
    "eslint": "^1.10.2",
    "eslint-config-google": "^0.3.0",
    "gulp": "^3.9.0",
    "gulp-babel": "^6.1.0",
    "run-sequence": "^1.1.4"
  }
}
